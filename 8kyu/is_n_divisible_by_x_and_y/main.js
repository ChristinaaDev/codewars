function isDivisible(n, x, y) {

    // check if n is divisible by two numbers
      if(n % x == 0 && n % y == 0){
        return true
      }else{
        return false
      }
        
    }